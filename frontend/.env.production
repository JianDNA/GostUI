# 生产环境配置
# 注意：不要在 .env 文件中设置 NODE_ENV=production，Vite 会自动处理

# API 配置 (生产环境中应该使用相对路径或实际域名)
VITE_API_BASE_URL=/api

# 构建配置
VITE_BUILD_SOURCEMAP=false
VITE_BUILD_MINIFY=true

# 调试配置
VITE_DEBUG=false
